local Player = game:GetService("Players").LocalPlayer
local BlackListTable = {
	[2788073752] = {
		["reason"] = "IceHub is kid ? U don't use IceHub ? lol we have execute log for anti toxic go away and fuck you bitch don't use IceHub loser shit."
	};
}

local BlackListCheck = BlackListTable[Player.UserId]

if BlackListCheck then
	Player:Kick("Blacklisted Reason: "..BlackListTable[Player.UserId]["reason"])
end
